<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login</title>
    <style>
      body {
        font-family: sans-serif;
        margin: 0;
        padding: 0;
        background-color: rgb(24, 200, 200);
        display: grid;
        place-items: center;
        min-height: 100vh;
      }

      .container {
        width: 400px;
        background-color: #fff;
        padding: 30px;
        box-shadow: 0px 0px 10px rgba(0, 0, 0, 2);
        border-radius: 10px;
      }

      h1 {
        text-align: center;
        margin-bottom: 30px;
        font-size: 24px;
      }

      form {
        display: flex;
        flex-direction: column;
      }

      label {
        margin: 10px 0;
        display: block;
        font-size: 16px;
      }

      input {
        padding: 15px;
        border: 1px solid #ccc;
        border-radius: 5px;
        font-size: 16px;
        width: calc(100% - 32px);
      }

      button {
        padding: 15px;
        background-color: #007bff;
        color: #fff;
        border: none;
        border-radius: 5px;
        cursor: pointer;
        font-size: 16px;
      }

      .forget-pass-wrapper {
        text-align: right;
        margin: 20px 0;
      }

      .forget-pass {
        color: #007bff;
        text-decoration: none;
        font-size: 14px;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h1>Login</h1>
      <form class="login-form" action="/api/v1/auth/login" method="post">
        <label for="email">Email:</label>
        <input
          type="email"
          id="email"
          name="email"
          placeholder="blablabla@gmail.com"
          required
        />
        <label for="password">Password:</label>
        <input
          type="password"
          id="password"
          name="password"
          placeholder="*******"
          required
        />
        <div class="forget-pass-wrapper">
          <a href="/api/v1/forget-pass" class="forget-pass">Lupa Password?</a>
        </div>
        <button type="submit">Login</button>
      </form>
    </div>
  </body>
</html>
